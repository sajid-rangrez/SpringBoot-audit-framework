spring.application.name=audit_poc

# Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/audit?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=#Alite123

# Hibernate Configuration
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Envers Configuration (Optional tweaks)
# This adds a suffix like _AUD to audit tables (default is _AUD)
spring.jpa.properties.org.hibernate.envers.audit_table_suffix=_AUD
# CHANGE THIS TEMPORARILY

#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE